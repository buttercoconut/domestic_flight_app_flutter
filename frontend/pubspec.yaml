name: domestic_flight_app
description: A domestic flight booking app built with Flutter.
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.3.6
  http: ^0.13.6
  intl: ^0.18.0
  flutter_localizations:
    sdk: flutter
  provider: ^6.0.5
  cached_network_image: ^3.2.3
  flutter_svg: ^2.0.5
  # Add other dependencies as needed

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
